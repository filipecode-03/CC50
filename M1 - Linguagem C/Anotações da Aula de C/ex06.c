#include <stdio.h>

int main(void) {
    // Pega os números do usuário
    int x = ("x: ");
    int y = ("y: ");

    // Divide x por y
    float z = (float) x / (float) y;
    printf("%li\n", x + y);
}